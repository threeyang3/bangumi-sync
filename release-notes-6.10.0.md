## 新功能

- 以 Bangumi 条目 ID 作为本地文件唯一身份，新增双向注册表、冲突诊断、Markdown 报告导出和路径迁移预览。
- 默认保持简洁文件名；仅在不同 ID 生成同一路径时，优先按年份对称消歧，同年或缺少年份时追加 Bangumi ID。
- 新增始终追加年份、始终追加 ID 和自定义模板命名策略，并在设置页展示普通路径与冲突路径预览。

## 改进

- 统一同步、状态同步、用户数据导入和共享笔记的 ID 解析规则，旧版 `BangumiID`、规范 Bangumi 链接和封面路径仍可迁移识别。
- 文件名非法字符改用可读的全角字符，碰撞检测覆盖大小写、Unicode 兼容形式和 Windows 路径限制。
- 同步结果明确区分创建、更新、未变化、重命名、冲突解决、跳过与失败，并正确展示部分成功。
- 新增 Node.js 22 的 Ubuntu/Windows CI，覆盖 lint、测试和生产构建。

## 修复

- 修复 Issue #1：1989 与 2024 版《乱马1/2》等同名同类型条目不再互相覆盖或被错误标记为同步成功。
- 所有覆盖写入先校验目标文件 ID；不同 ID、身份冲突和重复 ID 会阻止写入并给出可操作错误。
- 批量重命名、创建和更新纳入事务，取消或失败后可恢复原路径、原内容并清理新文件。
- 保留用户手动重命名路径；旧路径默认只预览、不自动迁移，必须由用户显式选择。
- 修复正文中的无关 Bangumi 链接被误判为文件身份冲突，以及 401 等不可重试 HTTP 错误被重复请求的问题。
