# 6.12.1 Release Validation

> 历史文档：本文记录 6.12.1 的测试、CI、Sandbox、Tag、Release 和分支同步事实，不是当前实现契约。可复用模板见 [`docs/maintainer/REGRESSION_CHECKLIST.md`](../../maintainer/REGRESSION_CHECKLIST.md)。

## Scope

- Vault 操作租约覆盖准备、主事务和 post-commit 写入。
- 插件卸载取消旧 manager，阻止重载后的陈旧写入。
- 独立路径迁移纳入持久 recovery journal 和统一 rollback。
- 并发配置边界、journal 插件版本与开发依赖安全修复。

## Automated validation

- [x] `npm ci`
- [x] `npm run lint`
- [x] `npm run test`（36 个测试文件，292 项测试）
- [x] `npm run build`
- [x] `npm run docs:check`
- [x] `npm audit` 与 `npm audit --omit=dev`（0 vulnerabilities）
- [x] `git diff --check`
- [ ] PR 与 `main` 的 Ubuntu / Windows CI

## Sandbox

- [x] 使用 production `release/main.js`、`release/manifest.json`、`release/styles.css`
- [x] 插件 reload 成功（Obsidian CLI，插件版本 6.12.1，13 个命令，manager `idle`）
- [x] 模拟运行中 reload 后，新 manager 为 `idle`，旧 manager 返回 `ManagerShuttingDownError`
- [x] `obsidian dev:errors` 为 0
- [x] error-level console 为 0

## Frozen asset hashes

| Asset | SHA-256 |
| --- | --- |
| `release/main.js` | `4C7539A1897832F9906E85A17CF7F756C10D951A17D691F587902BC5CC771C6B` |
| `release/manifest.json` | `E961DB6916CD4B8A55606C615FC6C32037B3A14EF8F16970884893C48F0815A7` |
| `release/styles.css` | `3BAAAF8435C3C021A7A696F997AB30E7072FAC431DD5E54350DBE23825FF359E` |

## Release

- [ ] Tag `6.12.1` 指向冻结的 `main` release commit
- [ ] GitHub Release 非 draft、非 prerelease
- [ ] 三个 release assets 下载回验，与冻结 production build SHA-256 一致
- [ ] `main` 正常 merge 回 `adv`
- [ ] 合并后 `adv` Ubuntu / Windows CI 通过
